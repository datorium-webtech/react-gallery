ReactDOM.render(
    
    [
        e(Image, null, null)
    ],

    document.querySelector(".image-container")
);

// To create several elements inside 1 container / parent element, you should use a list (array) of React elements
// TASK:
// Create 3 react elements inside .image-container:
// 1. <span>, red background, "Text for this span"
// 2. <span>, red background, "Text number 2"
// 3. <div>, blue background, "Third react element"

// TIMELIMIT: 19:20
